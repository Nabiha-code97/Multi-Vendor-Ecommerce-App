import { createReducer } from "@reduxjs/toolkit";

// Placeholder until the Event feature (backend + fetch actions) is built.
const initialState = {
  isLoading: true,
};

export const eventReducer = createReducer(initialState, () => {});
