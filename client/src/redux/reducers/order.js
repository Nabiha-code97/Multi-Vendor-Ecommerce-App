import { createReducer } from "@reduxjs/toolkit";

// Placeholder until the Order feature (backend + fetch actions) is built.
const initialState = {
  isLoading: true,
};

export const orderReducer = createReducer(initialState, () => {});
